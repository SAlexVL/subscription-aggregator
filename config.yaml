server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "subscriptions"
  password: "subscriptions"
  name: "subscriptions"
  sslmode: "disable"

logging:
  level: "info"
